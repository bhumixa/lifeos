export const environment = {
  production: true,
  // Set to the deployed Railway API URL — finalized in the Deployment milestone
  // (docs/09-roadmap.md, Phase 6). Left as a same-origin relative path for now
  // so a misconfigured build fails loudly instead of silently hitting localhost.
  apiUrl: '/api',
};
